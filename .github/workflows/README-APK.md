# Build APK

Le build APK se fait en local avec Android Studio (voir README principal).

Pour automatiser plus tard : GitHub Actions + Android SDK (image `macos-latest` ou `ubuntu-latest` avec setup-java + android-actions).
